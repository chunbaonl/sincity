<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="eu.greenhouseinnovation.selly.common.model.dao.ChatImageDao">

    <!-- Queries: -->
    <insert id="saveChatImageData" parameterType="ChatImage" >
        INSERT INTO chat_images (id, sender_user_id, recipient_user_id, store_id, sent_date, image, thumb)
        VALUES
        (#{image.mId}, #{image.mSenderUserId}, #{image.mRecipientUserId}, #{image.mStoreId}, #{image.mSentDate}, #{image.mImage}, #{image.mThumb})
    </insert>
</mapper>
